[用户输入: "你好"]
       │
       ▼
[ main.py ] ──(调用)──► [ brain.py: chat() ]
                               │
       ┌───────────────────────┴───────────────────────┐
       ▼                                               ▼
1. 追加到 self.history                           2. 切片取出上下文
  [..., {user: 你好}]                              [System Prompt] + [最近20条历史]
                                                       │
                                                       ▼
                                               3. HTTP 流式请求
                                                  DeepSeek API
                                                       │
       ┌───────────────────────────────────────────────┘
       ▼
4. 收到打字机 Chunk ──► 实时 print() 打印到屏幕
       │
       ▼
5. 拼接完整回复 ──► 追加到 self.history [..., {assistant: 你好呀!}]
       │
       ▼
6. 序列化保存到本地 memory.json 硬盘文件（永不丢失记忆）